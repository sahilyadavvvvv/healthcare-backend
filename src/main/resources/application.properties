# ============================================================
# Healthcare Portal Application Configuration
# ============================================================

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Database Configuration

spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=GGwxiGLMUcQvNeZgILltrdbnVRfGAFLi
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=healthcarePortalSecretKeyForJwtTokenGeneration1234567890
jwt.access-token-expiration=3600000
jwt.refresh-token-expiration=604800000

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=yadavsahil696@gmail.com
spring.mail.password=ozor tzsf owcr zsgz
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:5173
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=Authorization,Content-Type,X-Requested-With,Accept
cors.allow-credentials=true

# Application Settings
app.listing-expiry-days=90
app.max-images-per-listing=10
app.max-cv-size-mb=5
app.max-image-size-mb=5
app.base-url=http://localhost:8080/api

# Logging
logging.level.com.healthcare.portal=DEBUG
logging.level.org.springframework.security=DEBUG
